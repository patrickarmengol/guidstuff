[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "guidstuff"
version = "0.0.1"
description = ""
dependencies = ["litestar", "sqlalchemy", "asyncpg", "alembic"]
